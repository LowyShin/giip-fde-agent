# Jikji generated artifacts — do not commit unless explicitly desired
.jikji/
.jikji_agent_map.md

# Python cache/artifacts
__pycache__/
*.py[cod]

# Block accidental project-specific cache roots (e.g. PYTHONPYCACHEPREFIX=giipdb)
giipdb/

# slack-bot runtime state (machine-local, must NOT be versioned) — dirty tree breaks rebase/push
slack-bot/.bot-threads.json
slack-bot/.task-state.json
slack-bot/tasklist.json
slack-bot/.csn-lang-cache.env

# slack-bot giip 계정 시크릿(SK) — 실값 비커밋, *.sample 만 추적
slack-bot/.secrets/*
!slack-bot/.secrets/*.sample.json

# giip-1063: 비용 로그 / 재시도 checkpoint 등 런타임 산출물 (머신 로컬, 커밋 금지)
.agent/runtime/

# gissue 스케줄러 — 배포별 실제 CSN 매핑(경로/사설 정보 포함 가능)은 비커밋, .example 만 추적
scripts/gissue/csn-projects.json
scripts/gissue/logs/
scripts/gissue/*.lock
# 보조 :07 스케줄러(giip #2645)의 실행 산출물 — 실행 로그 + 회차별 결과 JSON(머신 로컬, 커밋 금지)
scripts/gissue/audit-results/

# Docker 배포 — csn/sk/login_id 등 실값이 들어가는 로컬 .env, .example 만 추적
docker/.env
