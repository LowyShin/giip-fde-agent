{
  "name": "giipclaude-bot",
  "version": "2.0.0",
  "description": "giipclaude Bot — Slack bot that drives the claude CLI task workflow for any GIIP dev-agent workspace (no Anthropic API key needed). Project-agnostic; configured entirely via environment variables.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@slack/socket-mode": "^2.0.7",
    "dotenv": "^16.4.5"
  }
}
